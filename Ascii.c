#include <stdio.h>
#include <string.h>
int main()
{
    char alpha;
    printf("Enter letter in the alphabet:");
    scanf("%c",&alpha);
    printf("\n The ascii value of alphabet variable is:%d",alpha);
    return 0;
}